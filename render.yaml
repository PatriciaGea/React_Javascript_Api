services:
  - type: web
    name: react-javascript-api-backend
    runtime: node
    rootDir: api_users
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: MONGO_URI
        sync: false
      - key: PORT
        value: 3000
      - key: CORS_ORIGINS
        value: http://localhost:5173,https://patriciagea.github.io
